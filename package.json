{
    "name": "graphql-profile-project",
    "version": "1.0.0",
    "description": "A profile page displaying school information from GraphQL API",
    "scripts": {
        "start": "serve -s public",
        "build": "echo 'No build step required for this simple project'",
        "deploy": "gh-pages -d public"
    },
    "dependencies": {
        "gh-pages": "^6.3.0",
        "serve": "^14.2.4"
    }
}
